name: Continuous Integration

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "45 18 * * *"

concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref_name }}
  cancel-in-progress: true

env:
  SYMBOL_PACKAGE: false

jobs:
  build:
    name: Build
    env:
      build-configuration: Release
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/sliekens/gw2sdk/devcontainer:latest
      options: --user 1001
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # (no history limit)
          filter: tree:0

      - name: Install dependencies
        run: dotnet restore --locked-mode

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Update test data
        if: github.event_name != 'pull_request'
        run: dotnet run --project src/GuildWars2.TestDataHelper
          --configuration Release
          --no-build
          -- artifacts/Data

      - name: Copy test data to all test directories
        if: github.event_name != 'pull_request'
        run: |
          for d in artifacts/bin/GuildWars2.Tests/*; do
            if [ -d "$d" ]; then
              cp -rf artifacts/Data/* "$d/Data"
            fi
          done

      - name: Test
        run: dotnet test
          --configuration Release
          --no-build
          --config-file tests/testconfig.json
          --coverage
          --no-progress
        env:
          ApiKeyBasic: ${{ secrets.API_TOKEN_BASIC }}
          ApiKey: ${{ secrets.API_TOKEN_FULL }}
          GuildLeader__Token: ${{ secrets.API_TOKEN_GUILD_LEADER }}

      - name: Upload test data on failure
        if: failure() && github.event_name != 'pull_request'
        uses: actions/upload-artifact@v6
        with:
          name: test-data
          path: artifacts/Data

      - name: Upload coverage
        uses: actions/upload-artifact@v6
        with:
          name: coverage
          path: artifacts/bin/GuildWars2.Tests/*/TestResults/coverage.xml

      - name: Upload coverage to Codecov
        if: github.event_name != 'pull_request'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Create packages
        if: github.event_name != 'pull_request'
        run: dotnet pack src/GuildWars2 --configuration Release --no-build

      - name: Upload artifacts
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v6
        with:
          name: package
          path: artifacts/package/

  deploy-to-github-packages:
    name: Deploy to GitHub Packages
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    environment:
      name: github-packages
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v7
        with:
          name: package
          path: artifacts

      - name: Upload packages to GitHub Packages
        run: dotnet nuget push artifacts/*/*.nupkg --source https://nuget.pkg.github.com/sliekens/index.json --api-key ${{secrets.GITHUB_TOKEN}} --skip-duplicate
